import React, { lazy, Suspense, useEffect, useState } from 'react';
import { Tabs } from 'antd';

const { TabPane } = Tabs;

const ReportsView = (props) => {
    return (
        <>
            <p>Reports component goes here</p>
        </>
    );
};

export default ReportsView;
